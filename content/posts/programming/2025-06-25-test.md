---
title: TEST
subtitle: 테스트
date: 2025-06-26T00:53:00.000Z
categories:
  - programming
subCategory: 모바일 앱
tags:
  - NFP 디자인
  - NFP Design
demoTheme: nfp
demoCode:
  lang: html
  code: >-
    import React, { useState } from 'react';

    import { Calculator, Grid, Image, Monitor, Printer, Smartphone } from 'lucide-react';


    export default function GuideApp() {
      const [selectedCategory, setSelectedCategory] = useState('sns');
      const [gridSettings, setGridSettings] = useState({
        width: 1200,
        height: 800,
        columns: 12,
        rows: 8,
        gutterWidth: 20,
        gutterHeight: 20,
        marginLeft: 40,
        marginRight: 40,
        marginTop: 40,
        marginBottom: 40
      });

      // SNS 사이즈 가이드
      const snsGuides = [
        {
          platform: '인스타그램',
          items: [
            { name: '피드 정사각형', size: '1080 x 1080px', ratio: '1:1' },
            { name: '피드 세로', size: '1080 x 1350px', ratio: '4:5' },
            { name: '스토리', size: '1080 x 1920px', ratio: '9:16' },
            { name: '릴스', size: '1080 x 1920px', ratio: '9:16' },
            { name: '프로필 사진', size: '320 x 320px', ratio: '1:1' },
          ]
        },
        {
          platform: '유튜브',
          items: [
            { name: '썸네일', size: '1280 x 720px', ratio: '16:9' },
            { name: '채널 아트', size: '2560 x 1440px', ratio: '16:9' },
            { name: '프로필 사진', size: '800 x 800px', ratio: '1:1' },
            { name: '쇼츠', size: '1080 x 1920px', ratio: '9:16' },
          ]
        },
        {
          platform: '페이스북',
          items: [
            { name: '포스트 이미지', size: '1200 x 630px', ratio: '1.91:1' },
            { name: '커버 사진', size: '851 x 315px', ratio: '2.7:1' },
            { name: '프로필 사진', size: '170 x 170px', ratio: '1:1' },
            { name: '스토리', size: '1080 x 1920px', ratio: '9:16' },
          ]
        }
      ];

      // 웹 사이즈 가이드
      const webGuides = [
        {
          category: '배너',
          items: [
            { name: '메인 배너', size: '1920 x 600px', ratio: '3.2:1' },
            { name: '서브 배너', size: '1200 x 400px', ratio: '3:1' },
            { name: '사이드 배너', size: '300 x 600px', ratio: '1:2' },
            { name: '하단 배너', size: '728 x 90px', ratio: '8:1' },
          ]
        },
        {
          category: '아이콘',
          items: [
            { name: '파비콘', size: '32 x 32px', ratio: '1:1' },
            { name: '애플 터치 아이콘', size: '180 x 180px', ratio: '1:1' },
            { name: '앱 아이콘', size: '512 x 512px', ratio: '1:1' },
            { name: 'UI 아이콘', size: '24 x 24px', ratio: '1:1' },
          ]
        }
      ];

      // 인쇄물 사이즈 가이드
      const printGuides = [
        {
          category: '명함/카드',
          items: [
            { name: '일반 명함', size: '90 x 50mm', ratio: '1.8:1' },
            { name: '미국 명함', size: '89 x 51mm', ratio: '1.75:1' },
            { name: '정사각 명함', size: '70 x 70mm', ratio: '1:1' },
            { name: '쇼핑백 카드', size: '85 x 55mm', ratio: '1.55:1' },
          ]
        },
        {
          category: '포스터',
          items: [
            { name: 'A4 포스터', size: '210 x 297mm', ratio: '1:1.41' },
            { name: 'A3 포스터', size: '297 x 420mm', ratio: '1:1.41' },
            { name: 'B2 포스터', size: '515 x 728mm', ratio: '1:1.41' },
            { name: '영화 포스터', size: '686 x 1016mm', ratio: '2:3' },
          ]
        }
      ];

      const categories = [
        { id: 'sns', name: 'SNS', icon: Smartphone, data: snsGuides },
        { id: 'web', name: '웹', icon: Monitor, data: webGuides },
        { id: 'print', name: '인쇄', icon: Printer, data: printGuides },
        { id: 'grid', name: '그리드 계산', icon: Grid, data: [] }
      ];

      const getCurrentData = () => {
        return categories.find(cat => cat.id === selectedCategory)?.data || [];
      };

      const handleCopySize = (size) => {
        navigator.clipboard.writeText(size);
        alert(`${size} 사이즈가 복사되었습니다.`);
      };

      const calculateGrid = () => {
        const { width, height, columns, rows, gutterWidth, gutterHeight, marginLeft, marginRight, marginTop, marginBottom } = gridSettings;
        
        const contentWidth = width - marginLeft - marginRight;
        const contentHeight = height - marginTop - marginBottom;
        
        const columnWidth = (contentWidth - (gutterWidth * (columns - 1))) / columns;
        const rowHeight = (contentHeight - (gutterHeight * (rows - 1))) / rows;
        
        return {
          columnWidth: Math.round(columnWidth * 100) / 100,
          rowHeight: Math.round(rowHeight * 100) / 100,
          contentWidth,
          contentHeight
        };
      };

      const gridCalc = calculateGrid();

      const GridVisualization = () => {
        const { width, height, columns, rows, gutterWidth, gutterHeight, marginLeft, marginRight, marginTop, marginBottom } = gridSettings;
        const scale = Math.min(300 / width, 200 / height);
        const scaledWidth = width * scale;
        const scaledHeight = height * scale;
        
        return (
          <div className="bg-gray-900 p-4 rounded-lg">
            <h3 className="text-white font-semibold mb-4">그리드 미리보기</h3>
            <div className="flex justify-center">
              <svg width={scaledWidth} height={scaledHeight} className="border border-gray-600">
                {/* 배경 */}
                <rect width={scaledWidth} height={scaledHeight} fill="#1a1a1a" />
                
                {/* 마진 영역 */}
                <rect 
                  x={marginLeft * scale} 
                  y={marginTop * scale} 
                  width={(width - marginLeft - marginRight) * scale} 
                  height={(height - marginTop - marginBottom) * scale} 
                  fill="none" 
                  stroke="#01FF75" 
                  strokeWidth="1"
                  strokeDasharray="2,2"
                />
                
                {/* 그리드 셀 */}
                {Array.from({ length: rows }, (_, rowIndex) => 
                  Array.from({ length: columns }, (_, colIndex) => {
                    const x = marginLeft + colIndex * (gridCalc.columnWidth + gutterWidth);
                    const y = marginTop + rowIndex * (gridCalc.rowHeight + gutterHeight);
                    
                    return (
                      <rect
                        key={`${rowIndex}-${colIndex}`}
                        x={x * scale}
                        y={y * scale}
                        width={gridCalc.columnWidth * scale}
                        height={gridCalc.rowHeight * scale}
                        fill="#01FF75"
                        fillOpacity="0.3"
                        stroke="#01FF75"
                        strokeWidth="1"
                      />
                    );
                  })
                )}
              </svg>
            </div>
          </div>
        );
      };

      const GridCalculator = () => (
        <div className="space-y-6">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            {/* 설정 패널 */}
            <div className="bg-gray-900 p-6 rounded-lg">
              <h3 className="text-white font-semibold mb-4 flex items-center gap-2">
                <Calculator className="w-5 h-5" />
                그리드 설정
              </h3>
              
              <div className="space-y-4">
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">너비 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.width}
                      onChange={(e) => setGridSettings({...gridSettings, width: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">높이 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.height}
                      onChange={(e) => setGridSettings({...gridSettings, height: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                </div>
                
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">열 개수</label>
                    <input
                      type="number"
                      value={gridSettings.columns}
                      onChange={(e) => setGridSettings({...gridSettings, columns: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">행 개수</label>
                    <input
                      type="number"
                      value={gridSettings.rows}
                      onChange={(e) => setGridSettings({...gridSettings, rows: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                </div>
                
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">거터 너비 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.gutterWidth}
                      onChange={(e) => setGridSettings({...gridSettings, gutterWidth: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">거터 높이 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.gutterHeight}
                      onChange={(e) => setGridSettings({...gridSettings, gutterHeight: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                </div>
                
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">좌측 마진 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.marginLeft}
                      onChange={(e) => setGridSettings({...gridSettings, marginLeft: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">우측 마진 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.marginRight}
                      onChange={(e) => setGridSettings({...gridSettings, marginRight: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                </div>
                
                <div className="grid grid-cols-2 gap-4">
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">상단 마진 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.marginTop}
                      onChange={(e) => setGridSettings({...gridSettings, marginTop: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                  <div>
                    <label className="block text-gray-300 text-sm mb-2">하단 마진 (px)</label>
                    <input
                      type="number"
                      value={gridSettings.marginBottom}
                      onChange={(e) => setGridSettings({...gridSettings, marginBottom: Number(e.target.value)})}
                      className="w-full bg-gray-800 text-white border border-gray-600 rounded-lg px-3 py-2"
                    />
                  </div>
                </div>
              </div>
            </div>
            
            {/* 결과 및 미리보기 */}
            <div className="space-y-6">
              <GridVisualization />
              
              <div className="bg-gray-900 p-6 rounded-lg">
                <h3 className="text-white font-semibold mb-4">계산 결과</h3>
                <div className="space-y-3">
                  <div className="flex justify-between items-center">
                    <span className="text-gray-300">열 너비:</span>
                    <span className="text-green-400 font-mono">{gridCalc.columnWidth}px</span>
                  </div>
                  <div className="flex justify-between items-center">
                    <span className="text-gray-300">행 높이:</span>
                    <span className="text-green-400 font-mono">{gridCalc.rowHeight}px</span>
                  </div>
                  <div className="flex justify-between items-center">
                    <span className="text-gray-300">콘텐츠 너비:</span>
                    <span className="text-green-400 font-mono">{gridCalc.contentWidth}px</span>
                  </div>
                  <div className="flex justify-between items-center">
                    <span className="text-gray-300">콘텐츠 높이:</span>
                    <span className="text-green-400 font-mono">{gridCalc.contentHeight}px</span>
                  </div>
                </div>
              </div>
              
              <div className="bg-gray-900 p-6 rounded-lg">
                <h3 className="text-white font-semibold mb-4">CSS 코드</h3>
                <div className="bg-gray-800 p-4 rounded-lg">
                  <pre className="text-sm text-green-400 whitespace-pre-wrap">
    {`.grid-container {
      width: ${gridSettings.width}px;
      height: ${gridSettings.height}px;
      padding: ${gridSettings.marginTop}px ${gridSettings.marginRight}px ${gridSettings.marginBottom}px ${gridSettings.marginLeft}px;
      display: grid;
      grid-template-columns: repeat(${gridSettings.columns}, ${gridCalc.columnWidth}px);
      grid-template-rows: repeat(${gridSettings.rows}, ${gridCalc.rowHeight}px);
      gap: ${gridSettings.gutterHeight}px ${gridSettings.gutterWidth}px;
    }`}
                  </pre>
                </div>
                <button
                  onClick={() => {
                    const cssCode = `.grid-container {
      width: ${gridSettings.width}px;
      height: ${gridSettings.height}px;
      padding: ${gridSettings.marginTop}px ${gridSettings.marginRight}px ${gridSettings.marginBottom}px ${gridSettings.marginLeft}px;
      display: grid;
      grid-template-columns: repeat(${gridSettings.columns}, ${gridCalc.columnWidth}px);
      grid-template-rows: repeat(${gridSettings.rows}, ${gridCalc.rowHeight}px);
      gap: ${gridSettings.gutterHeight}px ${gridSettings.gutterWidth}px;
    }`;
                    navigator.clipboard.writeText(cssCode);
                    alert('CSS 코드가 복사되었습니다!');
                  }}
                  className="mt-3 bg-green-500 text-black px-4 py-2 rounded-lg hover:bg-green-400 transition-colors"
                >
                  CSS 복사
                </button>
              </div>
            </div>
          </div>
        </div>
      );

      const SizeCard = ({ item, platform }) => (
        <div 
          className="bg-gray-900 rounded-lg p-4 border-l-4 border-green-400 cursor-pointer hover:bg-gray-800 transition-colors"
          onClick={() => handleCopySize(item.size)}
        >
          <div className="flex justify-between items-center mb-2">
            <span className="text-white font-semibold">{item.name}</span>
            <span className="bg-gray-700 text-green-400 px-2 py-1 rounded text-xs font-semibold">
              {item.ratio}
            </span>
          </div>
          <div className="text-green-400 text-lg font-bold mb-1">{item.size}</div>
          {platform && (
            <div className="text-gray-400 text-sm">{platform}</div>
          )}
        </div>
      );

      return (
        <div className="min-h-screen bg-black text-white p-5">
          <div className="max-w-6xl mx-auto">
            {/* 헤더 */}
            <div className="text-center py-8">
              <h1 className="text-3xl font-black text-white mb-2">디자인 가이드</h1>
              <p className="text-gray-400">플랫폼별 사이즈 가이드 & 그리드 계산기</p>
            </div>

            {/* 카테고리 탭 */}
            <div className="flex bg-gray-900 rounded-xl p-1 mb-8">
              {categories.map((category) => {
                const Icon = category.icon;
                return (
                  <button
                    key={category.id}
                    className={`flex-1 flex items-center justify-center gap-2 py-3 px-4 rounded-lg transition-all ${
                      selectedCategory === category.id
                        ? 'bg-green-400 text-black'
                        : 'text-gray-400 hover:text-white'
                    }`}
                    onClick={() => setSelectedCategory(category.id)}
                  >
                    <Icon className="w-4 h-4" />
                    {category.name}
                  </button>
                );
              })}
            </div>

            {/* 콘텐츠 */}
            {selectedCategory === 'grid' ? (
              <GridCalculator />
            ) : (
              <div className="space-y-8">
                {getCurrentData().map((section, sectionIndex) => (
                  <div key={sectionIndex}>
                    <h2 className="text-xl font-bold text-green-400 mb-4">
                      {section.platform || section.category}
                    </h2>
                    
                    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
                      {section.items.map((item, itemIndex) => (
                        <SizeCard 
                          key={itemIndex} 
                          item={item} 
                          platform={section.platform}
                        />
                      ))}
                    </div>
                  </div>
                ))}

                {/* DPI 가이드 */}
                {selectedCategory === 'print' && (
                  <div>
                    <h2 className="text-xl font-bold text-green-400 mb-4">DPI 가이드</h2>
                    <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
                      <div className="bg-gray-900 rounded-lg p-6 text-center">
                        <h3 className="text-white font-semibold mb-2">웹용</h3>
                        <div className="text-green-400 text-2xl font-bold mb-1">72 DPI</div>
                        <p className="text-gray-400 text-sm">화면 표시용</p>
                      </div>
                      <div className="bg-gray-900 rounded-lg p-6 text-center">
                        <h3 className="text-white font-semibold mb-2">인쇄용</h3>
                        <div className="text-green-400 text-2xl font-bold mb-1">300 DPI</div>
                        <p className="text-gray-400 text-sm">고품질 인쇄</p>
                      </div>
                      <div className="bg-gray-900 rounded-lg p-6 text-center">
                        <h3 className="text-white font-semibold mb-2">대형 인쇄</h3>
                        <div className="text-green-400 text-2xl font-bold mb-1">150 DPI</div>
                        <p className="text-gray-400 text-sm">현수막, 포스터</p>
                      </div>
                    </div>
                  </div>
                )}

                {/* 팁 섹션 */}
                <div className="bg-gray-900 rounded-xl p-6">
                  <h3 className="text-green-400 text-lg font-bold mb-3">💡 디자인 팁</h3>
                  <div className="text-white space-y-2">
                    <p>• 사이즈를 클릭하면 클립보드에 복사됩니다</p>
                    <p>• 비율(ratio)을 참고해서 다른 크기로 응용 가능합니다</p>
                    <p>• 인쇄물은 항상 여백(bleed)을 고려하세요</p>
                    <p>• 웹용 이미지는 용량 최적화를 잊지 마세요</p>
                  </div>
                </div>
              </div>
            )}
          </div>
        </div>
      );
    }
---
TEST1
